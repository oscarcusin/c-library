#include "weighted_graph.h"

#ifndef MINIMUM_SPANNING_TREE_H
#define MINIMUM_SPANNING_TREE_H

// Returns the minimum spanning tree of the weighted graph WG.
// The minimum spanning tree is a new weighted graph with the same vertices as WG, and a subset of the edges of WG.
weighted_graph * minimum_spanning_tree(weighted_graph * wg);

#endif
